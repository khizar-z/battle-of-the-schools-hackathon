{
  "name": "gehackathon",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "dev:server": "pnpm --filter @gehackathon/server dev",
    "dev:extension": "pnpm --filter @gehackathon/extension dev",
    "build:shared": "pnpm --filter @gehackathon/shared build",
    "build": "pnpm -r build",
    "typecheck": "pnpm run build:shared && pnpm -r typecheck",
    "test": "pnpm run build:shared && pnpm -r test",
    "lint": "pnpm run build:shared && pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
